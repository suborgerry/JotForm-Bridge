{
    "name": "jotform-bridge/jotform-bridge",
    "description": "Development harness for the Jotform Bridge WordPress plugin. Not shipped with the release ZIP.",
    "type": "project",
    "license": "GPL-2.0-or-later",
    "require": {
        "php": ">=8.0"
    },
    "require-dev": {
        "brain/monkey": "^2.6",
        "dealerdirect/phpcodesniffer-composer-installer": "^1.0",
        "phpunit/phpunit": "^9.6",
        "squizlabs/php_codesniffer": "^3.9",
        "wp-coding-standards/wpcs": "^3.1"
    },
    "autoload": {
        "psr-4": {
            "JotformBridge\\": "jotform-bridge/src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "JotformBridge\\Tests\\": "tests/"
        }
    },
    "scripts": {
        "test": "phpunit",
        "test:integration": [
            "@wp:install",
            "phpunit -c phpunit.integration.xml.dist"
        ],
        "wp:install": "bin/install-wp.sh",
        "lint": "phpcs",
        "lint:fix": "phpcbf",
        "hooks": "php bin/generate-hooks.php",
        "hooks:check": "php bin/generate-hooks.php --check",
        "version": "php bin/version.php",
        "version:check": "php bin/version.php --check",
        "check": [
            "@lint",
            "@hooks:check",
            "@version:check",
            "@test"
        ]
    },
    "scripts-descriptions": {
        "test": "Run the unit suite. No WordPress, no network.",
        "test:integration": "Run the integration suite against a real WordPress, downloading it first if needed.",
        "wp:install": "Download the WordPress the integration suite runs against into .wordpress/.",
        "lint": "Check the plugin against phpcs.xml.dist.",
        "lint:fix": "Apply the fixes phpcs can make on its own.",
        "hooks": "Regenerate HOOKS.md from the docblocks in src/.",
        "hooks:check": "Fail if HOOKS.md no longer matches the source.",
        "check": "Lint, hooks, versions, then the unit suite. The integration suite is a separate command because it downloads WordPress."
    },
    "config": {
        "sort-packages": true,
        "allow-plugins": {
            "dealerdirect/phpcodesniffer-composer-installer": true
        }
    }
}
