# CLAUDE.md

The specification for this repository lives in `AGENTS.md`, and it is imported
below rather than copied. One file is the source of truth: a second copy would
drift, and a spec that disagrees with itself is worse than no spec — this
project has already been bitten by documentation describing code that had moved
on.

@AGENTS.md
